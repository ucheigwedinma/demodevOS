import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'pro.developeros.app',
  appName: 'developerOS',
  webDir: 'build',
  server: {
    // For dev: uncomment and set to your local dev server URL
    // url: 'http://192.168.x.x:5173',
    // cleartext: true,
  },
};

export default config;
