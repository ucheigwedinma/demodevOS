import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'developeros.console',
  appName: 'developerOS-console',
  webDir: 'build'
};

export default config;
